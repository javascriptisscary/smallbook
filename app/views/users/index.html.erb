<h1>Users List</h1>

<div class="container">
  <% for user in @users %>
    <p>
      <% next if user.id ==current_user.id %>
      <%= image_tag user.avatar.url(:status) %>
      <%= link_to user.full_name, user_path(user.id) %>
      
    </p>
  <% end %>

</div>